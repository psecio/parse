{
	"checks": [
		{
			"description": "Verify that the calls to extract have the second paramater. This prevents overwrites of the local variables.",
			"match": "func:extract[argcount{<2}]"
		},
		{
			"description": "Don't use eval. Ever.",
			"match": "type:expr.eval"
		}
	]
}